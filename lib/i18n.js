export const LANGUAGES = {
  en: 'English',
  // Add other languages as needed
};

export const Locale = {
  // Add locale settings as neededase fix that
};

export function isRTL(language) {
  // Implement the isRTL function as needed
  return false;
}

export function translate(key, language) {
  // Implement the translate function as needed
  return key;
}